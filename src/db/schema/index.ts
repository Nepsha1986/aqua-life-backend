// Barrel of all Drizzle tables, so the db client (and Better Auth's adapter)
// see the full schema for relational queries and migrations.
export * from './merchants.ts';
export * from './stores.ts';
export * from './auth.ts';
